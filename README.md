# Murchalka Client Runtime

Universal product-agnostic client protocol host. Phase 5 provides authenticated realtime transport and declarative Agent UI documents; signed extension catalogs and WASM activation are added in Phase 7.

Canonical `vX.Y.Z` tags publish deterministic NuGet packages and an immutable GitHub Release.

